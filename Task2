#include <iostream>
using namespace std;

int main()
{
    double x;

    cin >> x;

    if (x < 0)
        cout << -(x * x) / (x - 1) << endl;
    else if (x >= 0)
        cout << (x * x) / (x + 1) << endl;
    else
        cout << 0 << endl;

    return 0;
}
